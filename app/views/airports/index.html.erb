<section>
    <div class="container">
        <div class="row mx-auto text-align-center mt-5 mb-5">
            <div class="col text-center">
                <h1>Airports List</h1>
            </div>
            <div class="col text-center mt-2">
                <%= link_to "Create New", new_airport_path, class: "btn btn-primary" %>
            </div>
        </div>

        <div class="row">
            <div class="col-12">
                <% @airports.each do |airport| %>
                    <div class="row">
                        <div class="col-4">
                            <%= link_to airport_path(airport), class: "text-decoration-none" do %>
                                <p class="fs-5"><%= airport.airport %></p>
                            <% end %>
                        </div>
                        <div class="col text-start">
                            <%= link_to "Click to edit", edit_airport_path(airport), class: "btn btn-success" %>
                        </div>
                    </div>
                <% end %>
            </div>
        </div>
    </div>
</section>